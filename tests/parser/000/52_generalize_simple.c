// Iteration 16: Generalization - simple parameter
int process(int x, int y) {
    return x + y;
}

int main() {
    int r1 = process(1, 2);
    int r2 = process(3, 4);
    return r1 + r2;
}
