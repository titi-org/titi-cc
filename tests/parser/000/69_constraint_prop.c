// Iteration 19: Transformation library - constraint propagation
int constrained(int x, int y) {
    int a = x + 0;
    int b = a * 1;
    int c = b + y;
    return c;
}

int main() {
    return constrained(5, 10);
}
